1)vim ~/.bashrc  -to open source file and add virtual env

2)create gitignore file then commands->
 git add *
 git status
 git commit -m "add strcuture"
 git push
3)add commands to makefile to install all packages at once  command -> make install
4)pip freeze - to view current versions of software

First step of continuous integration(CI)
5)go to actions inside repo -->under deployment -->choose Deploy to Amazon ECS and remove the contens--> add contents from  noahgift/github-actions-pytest   --->commit(start continuous integration)-->go to actions and watch the build
6)FOLLOW the makefile 
add format command in makefile
add code in logic.py 
give command -> make format
logic.py code is formatted
